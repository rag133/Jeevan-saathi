// Import the test component to see the new date-time picker
import { TestDateTimePicker } from '../components';

// Temporarily show the test component to see the new implementation
const KaryScreen = () => {
  return <TestDateTimePicker />;
};

export default KaryScreen;
